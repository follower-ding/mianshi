[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — React Router client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
